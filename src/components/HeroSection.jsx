import React from 'react'

const HeroSection = () => {
  return (
    <section 
    className="h-screen bg-gradient-to-b from-violet-900 to-black flex items-center 
    justify-between flex-col-reverse xl:flex-row px-10 lg:px-24 relative overflow-hidden"
    >
        {/* Left Section */}
        <div>
            <h1></h1>
        </div>

    </section>
  )
}

export default HeroSection